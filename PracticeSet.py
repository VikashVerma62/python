a={1,2,3}
print(a)

b={2.3,4.5}
print(b)

c={"a","b","c"}
print(c)

# d={1,23,["a","b","c"]}     #A set can only contain hashable (immutable) elements. A list (["a", "b", "c"]) is mutable, so it cannot be an element of a set.
# print(d)

# e={1,34,54,(1,2,3)}     #A set can only contain hashable (immutable) elements. A list (["a", "b", "c"]) is mutable, so it cannot be an element of a set.
# print(e)

# f={1,2,3,{"a":1,"b":2}}   #cannot use 'dict' as a set element (unhashable type: 'dict')
# print(f)

